<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:orientation="vertical"
    android:background="#CCCCCC"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.webviewapp.MainActivity"
    tools:ignore="MergeRootFrame" >
    
    <TextView
        android:id="@+id/email_details"
        android:layout_width="match_parent"
        android:layout_marginTop="10dp"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center"
        android:gravity="center|center"
        android:textSize="20dp"
        android:text="Please enter your email address. It will be used as a unique Username (We will not give out your email or contact you.)" />
    
    <EditText
        android:id="@+id/email_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="90dp"
        android:layout_gravity="top|center"
        android:hint="Enter a valid email"
        android:inputType="textEmailAddress"
        />
    
    <Button 
        android:id="@+id/confirm"
        android:layout_width= "match_parent"
        android:layout_height="40dp"
        android:layout_gravity="top|center"
        android:layout_marginTop="130dp"
        android:text="Confirm"
        android:textColor="#FFFFFF"
        android:background="@drawable/btn_default_green"
        />
</FrameLayout>
